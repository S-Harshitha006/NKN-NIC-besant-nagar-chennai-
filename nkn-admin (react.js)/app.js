/**
 * Legacy frontend removed.
 *
 * This file previously contained a plain-JavaScript UI implementation.
 * The project now uses React under `src/` and `index.html` loads the
 * React entry point (`/src/main.jsx`).
 *
 * Keeping this placeholder avoids accidental runtime errors if the
 * file is referenced somewhere. Remove the file entirely if you want
 * no trace of the legacy implementation.
 */

// No-op placeholder to avoid runtime/script errors.
